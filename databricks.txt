databricks
1)upload uc volumn file in catalog like pdf
there you need to create new schema and volume

2)goto workspace and create notebook
ctrl+i
generate sql for create table to hold extracted text of pdf document with autogenerated id and doc_text columns with delta.enablechangedatafeed as true

spark.sql("""
CREATE TABLE pdf_extracted_data (
  id BIGINT GENERATED ALWAYS AS IDENTITY,
  doc_text STRING
)
USING DELTA
TBLPROPERTIES (delta.enableChangeDataFeed = true)
""")

execute it

+code
ctrl+i
create table sql to track which pdf docs are already processed with filename column with enablechangedatafeed as true
spark.sql("""
CREATE TABLE pdf_processed_docs (
  id BIGINT GENERATED ALWAYS AS IDENTITY,
  filename STRING
)
USING DELTA
TBLPROPERTIES (delta.enableChangeDataFeed = true)
""")
accept and run

both table should be created in workspace>default> 

---------------------------
create notebook to pdf to doc_text tables
extract, chunks and store in table

---------------
create vector search end point
compute>> VECTOR SEARCH_> CREATE endpoint

create index
catalog> table > create vector search index on doc_text field
------------------------


RAG retriever
notebook


chatbot-access-token1
token
settings >developer
generate access token



for serving endpoint
go to AI/ML
models
serve the model




query:
{
  "dataframe_split": {
    "columns": [
      "query"
    ],
    "data": [
      
        "What does a FCC radiation message mean?"
      
    ]
  }
}